```{=html}
<% for (let i = 0; i < items.length; i++) {
    const item = items[i];
%>

<article class="research-item <%= i % 2 !== 0 ? 'reverse' : '' %>">

  <div class="research-content">

    <h2 class="research-title">
      <a href="<%= item.path %>">
        <%= item.title %>
      </a>
    </h2>

    <% if (item.categories && item.categories.length) { %>
      <div class="research-tags">
        <% item.categories.forEach(cat => { %>
          <span class="tag"><%= cat %></span>
        <% }) %>
      </div>
    <% } %>

    <% if (item.description) { %>
      <p class="research-description">
        <%= item.description %>
      </p>
    <% } %>

    <a class="research-link" href="<%= item.path %>">
      View Project →
    </a>

  </div>

  <% if (item.image) { %>
    <div class="research-image">
      <img
        src="<%= item.image %>"
        alt="<%= item['image-alt'] || item.title %>"
        loading="lazy"
      />
    </div>
  <% } %>

</article>

<% } %>

```

<style>
.simple-card {
  display: flex;
  max-width: 1200px;
  height: 200px;
  margin: 2rem 2rem;
  border-radius: 5px;
  overflow: hidden;
  background-color: #dddddd;
  /* The above gradient goes from light gray -> white -> light gray */
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
  font-family: 'Segoe UI', sans-serif;
  border: 1.5px solid #c0c1c4;
  transition: all 0.3s ease;
  position: relative;
}

.simple-card.reverse {
  flex-direction: row-reverse;
}

.card-text {
  flex: 1;
  padding: 1.5rem 1.5em;
  display: flex;
  flex-direction: column;
  justify-content: center;
  background: transparent;
}

.title {
  margin: 0 0 0.5rem;
  font-size: 1.25rem;
  color: #Fe5c00;
}

.title a {
  color: inherit;
  text-decoration: none;
}

.meta {
  font-size: 0.85rem;
  color: #6b7280;
  margin: 0 0 0.4rem;
}

.categories {
  margin: 0 0 0.1rem;
}

.category {
  background-color: #000000;
  color: #ffffff;
  padding: 0.3rem 0.6rem;
  border-radius: 9999px;
  font-size: 0.75rem;
  margin-right: 0.4rem;
  display: inline-block;
}

.description {
  font-size: 0.95rem;
  color: #374151;
  line-height: 1.4;
  margin: 0;
  max-height: 200px; /* Adjust as needed */
}

.card-image {
  width: 300px;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  /* background: #f9fafb; */
  background: transparent;
}

.card-image img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
  display: block;
}

@media (max-width: 768px) {
  .simple-card {
    flex-direction: column;
    height: auto;
  }

  .card-image {
    width: 100%;
    height: 200px;
  }

  .card-image img {
    max-height: 100%;
    object-fit: contain;
  }
}
</style>
```